/** Single-hue chart theme for Curo's magnitude charts (all single-series, so
 * one consistent brand hue is used throughout rather than a categorical set —
 * see the dataviz guidance: identity encoding isn't needed when the axis
 * labels already name each category. */
export const CHART_COLOR = '#c74504';
export const CHART_GRID_COLOR = '#e7e5e4';
export const CHART_AXIS_COLOR = '#8a8a8a';
export const CHART_TEXT_COLOR = '#555555';
